/**
 * 패널이 화면 가장자리에서 떨어진 거리 — 상세 카드가 옆 패널을 비켜 서는 계산에 쓴다.
 * 패널·헤더·커맨드 바는 이 값을 유틸리티(`left-4`·`right-4`·`inset-x-4` = 16px)로 두므로 함께 움직여야 한다.
 * 클래스 이름은 문자열이라 이 상수에서 만들 수 없어 값을 두 벌로 둔다.
 */
export const PANEL_MARGIN = 16

/**
 * 기준점 목록 패널의 폭.
 *
 * <p>회원 지도에서는 패널이 위에 선 알약 폭을 따라가고, 그 값이 이 수다(브랜드 글자 + 탭 둘).
 * 탭이 하나뿐인 화면은 알약이 짧지만 목록이 담는 것은 같으므로 패널은 이 폭을 그대로 쓴다.
 *
 * <p>알약 폭이 바뀌면(탭 이름·브랜드 글자) 회원 지도의 패널만 따라 움직여 두 화면이 어긋난다.
 * 그때는 이 값도 함께 고친다.
 */
export const PANEL_WIDTH = 397
